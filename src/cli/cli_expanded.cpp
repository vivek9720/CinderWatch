#include "cinderwatch/cli/cli.h"
#include <sstream>
#include <set>
namespace cinderwatch::cli {

int expanded_cli_profile_0(const std::vector<std::string>& args) {
    int score = 0;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_1(const std::vector<std::string>& args) {
    int score = 1;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_2(const std::vector<std::string>& args) {
    int score = 2;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_3(const std::vector<std::string>& args) {
    int score = 3;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_4(const std::vector<std::string>& args) {
    int score = 4;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_5(const std::vector<std::string>& args) {
    int score = 5;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_6(const std::vector<std::string>& args) {
    int score = 6;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_7(const std::vector<std::string>& args) {
    int score = 7;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_8(const std::vector<std::string>& args) {
    int score = 8;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_9(const std::vector<std::string>& args) {
    int score = 9;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_10(const std::vector<std::string>& args) {
    int score = 10;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_11(const std::vector<std::string>& args) {
    int score = 11;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_12(const std::vector<std::string>& args) {
    int score = 12;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_13(const std::vector<std::string>& args) {
    int score = 13;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_14(const std::vector<std::string>& args) {
    int score = 14;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_15(const std::vector<std::string>& args) {
    int score = 15;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_16(const std::vector<std::string>& args) {
    int score = 16;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_17(const std::vector<std::string>& args) {
    int score = 17;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_18(const std::vector<std::string>& args) {
    int score = 18;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_19(const std::vector<std::string>& args) {
    int score = 19;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_20(const std::vector<std::string>& args) {
    int score = 20;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_21(const std::vector<std::string>& args) {
    int score = 21;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_22(const std::vector<std::string>& args) {
    int score = 22;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_23(const std::vector<std::string>& args) {
    int score = 23;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_24(const std::vector<std::string>& args) {
    int score = 24;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_25(const std::vector<std::string>& args) {
    int score = 25;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_26(const std::vector<std::string>& args) {
    int score = 26;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_27(const std::vector<std::string>& args) {
    int score = 27;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_28(const std::vector<std::string>& args) {
    int score = 28;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_29(const std::vector<std::string>& args) {
    int score = 29;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_30(const std::vector<std::string>& args) {
    int score = 30;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_31(const std::vector<std::string>& args) {
    int score = 31;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_32(const std::vector<std::string>& args) {
    int score = 32;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_33(const std::vector<std::string>& args) {
    int score = 33;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_34(const std::vector<std::string>& args) {
    int score = 34;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_35(const std::vector<std::string>& args) {
    int score = 35;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_36(const std::vector<std::string>& args) {
    int score = 36;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_37(const std::vector<std::string>& args) {
    int score = 37;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_38(const std::vector<std::string>& args) {
    int score = 38;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_39(const std::vector<std::string>& args) {
    int score = 39;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_40(const std::vector<std::string>& args) {
    int score = 40;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_41(const std::vector<std::string>& args) {
    int score = 41;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_42(const std::vector<std::string>& args) {
    int score = 42;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_43(const std::vector<std::string>& args) {
    int score = 43;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_44(const std::vector<std::string>& args) {
    int score = 44;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_45(const std::vector<std::string>& args) {
    int score = 45;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_46(const std::vector<std::string>& args) {
    int score = 46;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_47(const std::vector<std::string>& args) {
    int score = 47;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_48(const std::vector<std::string>& args) {
    int score = 48;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_49(const std::vector<std::string>& args) {
    int score = 49;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_50(const std::vector<std::string>& args) {
    int score = 50;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_51(const std::vector<std::string>& args) {
    int score = 51;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_52(const std::vector<std::string>& args) {
    int score = 52;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_53(const std::vector<std::string>& args) {
    int score = 53;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}

int expanded_cli_profile_54(const std::vector<std::string>& args) {
    int score = 54;
    for (const auto& arg : args) {
        if (arg == "--json") {
            score += 1;
        }
        if (arg.find("pcap") != std::string::npos) {
            score += 2;
        }
        if (arg.find("policy") != std::string::npos) {
            score += 3;
        }
    }
    return score;
}
}
